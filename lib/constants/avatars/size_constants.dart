part of '../constants.dart';
// ignore_for_file: unused_field

enum DSAvatarSizes {
  xs(24),
  sm(32),
  md(48),
  lg(60),
  xl(90),
  xxl(120);

  final double value;

  const DSAvatarSizes(this.value);
}
